# scraping
playwright>=1.41.0
requests>=2.32.0

# OpenAI (Responses API 사용)
openai>=1.0.0

# optional but recommended (HTML 파싱을 쓰는 경우)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# optional (재시도/백오프를 코드에서 쓰면)
tenacity>=8.2.0
